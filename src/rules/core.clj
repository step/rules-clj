(ns rules.core)

(defn display
  "Displays each state on a separate line"
  [states])

(defn process
  "Given a initial state and the number of iterations, produces
   each consecutive state for as many iterations as specified"
  [init iterations]
  nil)

